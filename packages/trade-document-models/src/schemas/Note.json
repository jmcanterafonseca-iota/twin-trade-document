{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://schema.twindev.org/trade-document/Note",
	"title": "Note",
	"description": "Contractual prose with no typed UN/CEFACT slot. `subject` is mandatory: it is\nhow a consumer finds a note without parsing free text.",
	"type": "object",
	"properties": {
		"@context": {
			"$ref": "https://schema.twindev.org/unece/UneceContextType",
			"description": "JSON-LD Context."
		},
		"type": {
			"const": "Note",
			"description": "JSON-LD Type."
		},
		"content": {
			"type": "string",
			"description": "A content, expressed as text, of this note."
		},
		"subject": {
			"type": "string",
			"description": "The subject, expressed as text, of this note."
		}
	},
	"required": [
		"@context",
		"type",
		"content",
		"subject"
	],
	"allOf": [
		{
			"$ref": "https://schema.twindev.org/unece/UneceNote"
		}
	]
}
