{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://schema.twindev.org/trade-document/Price",
	"title": "Price",
	"description": "A unit price: the amount, and the quantity that amount is quoted against.\nThe basis is not the packing unit — a contract can price per 50 kg on 60 kg\nbags, and dropping the basis makes any total wrong.",
	"type": "object",
	"properties": {
		"@context": {
			"$ref": "https://schema.twindev.org/unece/UneceContextType",
			"description": "JSON-LD Context."
		},
		"type": {
			"const": "TradePrice",
			"description": "JSON-LD Type."
		},
		"unitAmount": {
			"type": "array",
			"items": {
				"$ref": "https://schema.twindev.org/trade-document/Amount"
			},
			"description": "The price amount and its currency."
		},
		"basisQuantity": {
			"$ref": "#/$defs/Quantity",
			"description": "The quantity the amount is quoted against."
		}
	},
	"required": [
		"@context",
		"type",
		"unitAmount",
		"basisQuantity"
	],
	"allOf": [
		{
			"$ref": "https://schema.twindev.org/unece/UneceTradePrice"
		}
	],
	"$defs": {
		"Quantity": {
			"title": "Quantity",
			"description": "A counted or measured quantity. The unit itself has no home: IUneceQuantityCode\ndeclares no value property, so `QuantityTypeCode` cannot carry \"KGM\".",
			"type": "object",
			"properties": {
				"@context": {
					"$ref": "https://schema.twindev.org/unece/UneceContextType",
					"description": "JSON-LD Context."
				},
				"type": {
					"const": "QuantityType",
					"description": "JSON-LD Type."
				},
				"QuantityTypeValue": {
					"type": "string",
					"description": "The numeric value."
				}
			},
			"required": [
				"@context",
				"type",
				"QuantityTypeValue"
			],
			"allOf": [
				{
					"$ref": "https://schema.twindev.org/unece/UneceQuantityType"
				}
			]
		}
	}
}
